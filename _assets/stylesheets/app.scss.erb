@import "_vendor/normalize";
@import "_vendor/foundation/settings";
@import "_vendor/foundation";
// @import "_vendor/custom_foundation";
@import "_vendor/foundation/social_foundicons";
@import "_vendor/bourbon/_bourbon.scss";
@import url(http://fonts.googleapis.com/css?family=Lato:300,700);
// @import url(http://fonts.googleapis.com/css?family=Maven+Pro:500);

$break-large: 90rem;
$break-medium: 64rem;
$break-small: 40rem;
$section-bg-blue: #6f91a3;
$section-bg-red: #B56942;

body {
  .page-container {
    display: inline;
    height: 100%;
    .off-canvas-wrap {
      height: 100%;
      overflow: inherit;
      .inner-wrap {
        height: 100%
      }
    }
    .move-right {
      overflow: hidden;
    }
  }
}

.show-onload {
  display: none;
}

.spacer {
  height: 2rem;
  @media screen and (max-width: $break-small) {
    height: 1rem;
  }
}

.bigger-spacer {
  height: 2rem;
}

.hero-photo {
  top: 0;
  width: 100%;
  min-height: 100%;
  background: url(<%= asset_path "sunlight_large@2x.jpg" %>) no-repeat center center fixed;
    -webkit-background-size: cover;
       -moz-background-size: cover;
         -o-background-size: cover;
            background-size: cover;
  @media screen and (max-width: $break-medium) {
    background: url(<%= asset_path "sunlight_medium@2x.jpg" %>) no-repeat center center fixed;
    -webkit-background-size: cover;
       -moz-background-size: cover;
         -o-background-size: cover;
            background-size: cover;
  }
  @media screen and (max-width: $break-small) {
    background: url(<%= asset_path "sunlight_small@2x.jpg" %>) no-repeat center center fixed;
    -webkit-background-size: cover;
       -moz-background-size: cover;
         -o-background-size: cover;
            background-size: cover;
  }
  .title-text {
    color: #FEFEFE;
    font-family: 'Lato', Helvetica, sans-serif;
    font-weight: 100;
    font-size: 3.5em;
    text-shadow: 0 1px rgba(123, 122, 122, 0.6);
    width: 100%;
    margin: 0;
    padding-top: .7rem;
    text-align: center;
    &.dark {
      color: #121212;
    }
    &.title-description {
      font-size: 2.3em
    }
    .emphasis {
      color: #0672AF;
    }
    strong {
      font-weight: 700;
    }
    @media screen and (max-width: $break-small) {
      font-size: 3em;
      margin-top: .5em;
    }
  }
  .social-icons {
    width: 100%;
    text-align: center;
    a {
      font-size: 3.5em;
      color: rgba(#0672AF, 0.7);
      @include transition(all 0.5s ease);
      &:hover {
        color: rgba(#EFEFEF, 0.7);
      }
    }
  }
}

.sticking-nav {
  right: 0;
  left: 0;
  top: 0;
  position: fixed;
  margin-top: 0 !important;
}

.nav-bar {
  background-color: rgba(100,100,100,0.4);
  .top-bar-section ul li {
    background: transparent;
  }
  margin-top: -45px;
  z-index: 100;
  font-family: 'Maven Pro', Helvetica, sans-serif;
  a {
    font-family: helvetica;
  }
  @media screen and (max-width: $break-small) {
    display: none;
  }
}

.sidenav {
  .left-off-canvas-menu {
    min-height: 100%;
  }
  .left-small {
    top: -5px;
    border-right: none;
  }
  h1 {
    margin-right: 2.6em;
  }
}

.row {
  font-family: 'Helvetica Neue', Helvetica, sans-serif;
  color: white;
  .section-title {
    height: 3rem;
    margin-bottom: 1rem;
    @media screen and (max-width: $break-small) {
      height: 0;
      margin-bottom: .5rem;
    }
  }
  .section-header {
  font-family: 'Helvetica Neue', Helvetica, sans-serif;
  font-size: 3rem;
  @media screen and (max-width: $break-small) {
    font-size: 1.5rem;
  }
    &.left {
      color: #FEFEFE;
    }
    &.right {
      color: #121212;
    }
  }
  &.content {
    margin-top: 5rem;
    @media screen and (max-width: $break-small) {
      margin-top: 1rem;
    }
  }
  p {
    font-size: 1.75rem;
    @media screen and (max-width: $break-medium) {
      font-size: 1.25rem;
    }
    @media screen and (max-width: $break-small) {
      font-size: 1.1rem;
    }
  }
  figure {
    margin-top: 0;
    width: 90%;
    text-align: center;
    @media screen and (max-width: $break-small) {
      width: 100%;
      margin-left: 0;
    }
    img {
      border-radius: 1rem;
      width: 100%;
      @media screen and (max-width: $break-small) {
        margin-top: 2rem;
        width: 65%;
      }
    }
  }
  figcaption {
      text-align: center;
  }
}

section {
  &.about {
    margin: 0;
    width: 100%;
    background-color: $section-bg-blue;
  }
  &.projects {
    margin: 0;
    width: 100%;
    background-color: $section-bg-red;
  }
  &.technologies {
    background-color: $section-bg-blue;
  }
  .content {
    p {
      margin-bottom: 2em;
      @media screen and (max-width: $break-small) {
        font-size: .9rem;
      }
    }
  }
  .nav-link {
    margin-top: 3em;
  }
}

.orbit-container {
  margin-top: 1em;
}